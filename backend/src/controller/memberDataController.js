export function getAllMembersController(req, res) {
    try{

    }catch(err){
        // if error, return the number 0 with status(400) for error.
    }
}

export function createMemberController(req, res){
    try{

    }catch(err){
        // success: true / false, message:. If success (status code) 200, else 400
    }
}

export function getMemberController(req, res){
    try{

    }catch(err){
        // if error, return a json object containing error: and error code. With status(400) for error.
    }
}

export function updateMemberController(req, res){
    try{

    }catch(err){
        // success: true / false, message:. If success (status code) 200, else 400
    }
}

export function deleteMemberController(req, res){
    try{

    }catch(err){
        // success: true / false, message:. If success (status code) 200, else 400
    }
}